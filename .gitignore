# Python
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
build/
dist/

# uv / virtualenv
.venv/
.python-version

# Editor / OS
.DS_Store
*.swp
*.swo

# Project archive + scratch conventions (per global CLAUDE.md / AGENTS.md §13)
archive/
tmp/

# Session handoffs (per AGENTS.md §11). This repo is PUBLIC, so the live
# handoff stays local: it can name a host, an internal URL or a credential.
restart.md
